    public %RETURN%.Array %VULKANFUNCTIONNAME%(%PARAMETERS%){
        super.%VKFUNCTIONNAME%(%ARGUMENTS%%COMMA%count.get(), null, result.get());
        if(result.get().getValue() != VK_SUCCESS) throw new VkException(result.get(), "%VKFUNCTIONNAME%");

        %RETURN%.Array o = new %RETURN%.Array(count.get().getValue());

        super.%VKFUNCTIONNAME%(%ARGUMENTS%%COMMA%count.get(), o, result.get());
        if(result.get().getValue() != VK_SUCCESS) throw new VkException(result.get(), "%VKFUNCTIONNAME%");

        return o;
    }