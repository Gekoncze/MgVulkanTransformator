### SOON


### NOW
    Vulkan level implementation
        wrap vk result functions with checks throwing vulkan exception
        pair handles with create / destroy functions into auto-closeable classes

### DONE
Cleanup + some early oop level classes and templates
    moved some files around in new packages
    renamed some packages
    added some oop level classes and templates
    renamed VulkanTranslator to CoreTranslator and divided it into C, Vk and Vulkan parts
    simplified Translator, VkTranslator
    moved all loaded templates into static final fields or maps