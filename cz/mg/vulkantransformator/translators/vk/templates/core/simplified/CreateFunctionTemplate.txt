    public %RETURN% %VULKANFUNCTIONNAME%(%PARAMETERS%){
        %RETURN% o = new %RETURN%();
        super.%VKFUNCTIONNAME%(%ARGUMENTS%, pAllocator, o, result.get());
        if(result.get().getValue() != VK_SUCCESS) throw new VkException(result.get(), "%VKFUNCTIONNAME%");
        return o;
    }