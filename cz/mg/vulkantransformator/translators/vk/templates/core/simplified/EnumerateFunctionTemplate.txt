    public %RETURN%.Array %VULKANFUNCTIONNAME%(%PARAMETERS%){
        VulkanResult rval = new VulkanResult();
        VulkanUInt32 count = new VulkanUInt32();

        vk.%VKFUNCTIONNAME%(%ARGUMENTS%%COMMA%count.getVk(), null, rval.getVk());
        if(rval.getValue() != VulkanResult.SUCCESS) throw new VulkanException(rval, "%VKFUNCTIONNAME%");

        %RETURN%.Array o = new %RETURN%.Array(count.getValue());

        vk.%VKFUNCTIONNAME%(%ARGUMENTS%%COMMA%count.getVk(), o.getVk(), rval.getVk());
        if(rval.getValue() != VulkanResult.SUCCESS) throw new VulkanException(rval, "%VKFUNCTIONNAME%");

        return o;
    }