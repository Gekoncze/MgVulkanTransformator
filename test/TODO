### SOON


### NOW
    Vulkan level implementation
        wrap vk result functions with checks throwing vulkan exception
        pair handles with create / destroy functions into auto-closeable classes

### DONE
Vulkan level implementation PART #2
    fixed bug in toString in flag bits classes
    fixed flag bits and enum value name conversion
    added flags, flag bits and enum translators + templates